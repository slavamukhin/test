import styled from 'styled-components'
import { Dropzone } from '@inno/ui-kit'

export const StyledDropzone = styled(Dropzone)`
  margin-top: -4rem;
`

export const Error = styled.div`
  margin-top: 2rem;
  font-weight: 400;
  font-size: 3.5rem;
  line-height: 4rem;
  white-space: pre-wrap;
  color: #d92020;
`