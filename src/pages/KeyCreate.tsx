import React, { FC } from 'react'
import { KeyCreateContent } from '../components'
import { Layout } from '../layout'

export const KeyCreate: FC = () => {
  return (
    <Layout>
      <KeyCreateContent />
    </Layout>
  )
}
